---
title: Webhooks and API (Business)
description: "Receive signed events on your server. Actions + custom data for your direct integration. Business plan."
---

import { Aside } from '@astrojs/starlight/components'

If your business requires custom integration with your own systems (ERP, custom CRM, your own marketplace), Brote exposes **outgoing webhooks** and a **REST API** — available on the **Business** plan ($349/mo).

<Aside type="note" title="Why Business only?">
Webhooks/API require specific infrastructure and support: rate limiting, retries with backoff, delivery logging, debugging when things break. We keep it on Business to guarantee an SLA and focus resources where they're needed. For cases without your own infrastructure, [Zapier](/en/integraciones/zapier/) covers you from Pro.
</Aside>

## Outgoing webhooks

Every Brote system event fires an HTTPS POST to the endpoints you configure, signed with **HMAC-SHA256**.

### Available events

```
conversation.created
conversation.updated
conversation.closed
message.received
message.sent
contact.created
contact.updated
contact.opted_out
ticket.created
ticket.stage_changed
ticket.won
ticket.lost
lead.qualified
payment.received
campaign.completed
agent.handoff
```

### Typical payload

```json
{
  "event": "payment.received",
  "timestamp": "2026-05-19T10:23:45.000Z",
  "organization_id": "uuid",
  "data": {
    "payment_id": "mp_payment_123",
    "amount": 49000,
    "currency": "CLP",
    "conversation_id": "uuid",
    "contact_id": "uuid",
    "contact": {
      "name": "María González",
      "phone": "+569·········",
      "email": "maria@ejemplo.cl"
    },
    "external_reference": "ticket_uuid"
  }
}
```

### Verifying the HMAC

Header `X-Brote-Signature: sha256=<hex>`. Compute on your server:

```js
const expected = crypto.createHmac('sha256', WEBHOOK_SECRET)
  .update(rawBody)
  .digest('hex')
const isValid = `sha256=${expected}` === req.headers['x-brote-signature']
```

If you don't validate the HMAC, anyone could POST fake events to you. Always validate.

### Retries + delivery log

If your endpoint responds with a 5xx or times out, Brote retries with exponential backoff: 1min, 5min, 30min, 2h, 12h. After that it's marked `failed`. All deliveries (successful and failed) are recorded in the **webhook_deliveries log**, viewable from the dashboard.

## REST API

Base endpoint: `https://api.brote.ai/v1/`

Auth: Bearer token (per-org API key, generated from **Settings → API Keys**).

### Main endpoints

```
GET    /v1/contacts              # listar contactos
POST   /v1/contacts              # crear contacto
GET    /v1/contacts/{id}         # detalle
PATCH  /v1/contacts/{id}         # actualizar

GET    /v1/conversations         # listar conv
POST   /v1/conversations/{id}/messages   # enviar mensaje desde sistema externo

GET    /v1/tickets               # listar tickets
PATCH  /v1/tickets/{id}/stage    # mover etapa

POST   /v1/payments/links        # crear link MP programático
GET    /v1/payments              # listar pagos

GET    /v1/campaigns             # listar campañas
POST   /v1/campaigns             # crear campaña vía API
```

### Rate limits

- **100 req/min** sustained on the Business plan.
- Occasional **bursts of 200**.
- If you exceed them: 429 with a `Retry-After` header.

## Next steps

- [Zapier (low-code alternative)](/en/integraciones/zapier/)
- [Slack notifications](/en/integraciones/slack/)
- [Plan comparison](/en/planes/comparativa/)
